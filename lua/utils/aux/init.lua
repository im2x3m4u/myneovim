local aux = require("utils.aux.aux")

aux.packer = require("utils.aux.packer")

aux.lspconfig = require("utils.aux.lspconfig")

aux.nvim_cmp = require("utils.aux.nvim-cmp")

-- aux.actions = require("utils.aux.actions")

return aux
